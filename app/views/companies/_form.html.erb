<%= form_with(model: company, local: true) do |form| %>
  <% if company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label "Назва" %>
    <%= form.text_field :code_name %>
  </div>

  <div class="field">
    <%= form.label "Назва ролі, що дозволяє доступ" %>
    <%= form.text_field :key_role %>
  </div>

  <div class="field">
    <%= form.label "Належить до групи" %>
    <%= form.select("group_id", Group.all.collect {|g| [ g.code_name_ukr+ " (#{g.code_name_eng})", g.id ] }) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
